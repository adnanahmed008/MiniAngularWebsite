export interface ICountry {
    /**
     * Name
     */
     name: string;
     /**
      * ISO Code
      */
     iso: string;
     /**
      * Dial Code
      */
     code: string;
     isSelected?: boolean;
}
